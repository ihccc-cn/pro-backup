{
  "name": "project",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "dev:main": "pro-backup main && vite",
    "dev:yaohui": "pro-backup yaohui && vite",
    "dev:goofo": "pro-backup goofo && vite",
    "dev:tuio": "pro-backup tuio && vite",
    "dev:goofo2": "pro-backup goofo2 && vite",
    "build": "vite build",
    "build:main": "pro-backup main && vite build",
    "build:yaohui": "pro-backup yaohui && vite build",
    "build:goofo": "pro-backup goofo && vite build",
    "build:tuio": "pro-backup tuio && vite build",
    "build:goofo2": "pro-backup goofo2 && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "pro-backup": "link:..",
    "vite": "^5.0.8"
  }
}
