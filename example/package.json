{
  "name": "project",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "dev:main": "pb main && vite",
    "dev:yaohui": "pb yaohui && vite",
    "dev:goofo": "pb goofo && vite",
    "dev:tuio": "pb tuio && vite",
    "dev:goofo2": "pb goofo2 && vite",
    "build": "vite build",
    "build:main": "pb main && vite build",
    "build:yaohui": "pb yaohui && vite build",
    "build:goofo": "pb goofo && vite build",
    "build:tuio": "pb tuio && vite build",
    "build:goofo2": "pb goofo2 && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "pro-backup": "link:..",
    "vite": "^5.0.8"
  }
}
